# AI 漫剧自动生产系统

## 项目目标
本项目用于在 Claude Code 中搭建一个可商用的 AI 漫剧生产工作流。
系统需要支持从创意定义、剧本开发、15 秒分段规划、图片资产生产，到视频段生产的完整流程。
所有关键结果必须可审核、可回退、可复用、可交接。

## 项目工作原则
1. 前期创意阶段采用助手式协作，后期生产阶段采用流水线式推进。
2. 所有关键产物必须文件化，不能只存在聊天上下文中。
3. 所有阶段必须先产出正式文件，再进入审核。
4. 所有阶段必须通过业务审核；关键节点必须通过合规审核。
5. 未经审核通过，不得进入下一阶段。
6. 严禁让低质量或不合规内容流入后续高成本阶段。
7. 主控负责调度，不负责具体专业产出。
8. 各专业 Agent 只负责本阶段职责，不得越权推进到其他阶段。
9. 如果信息不足，优先补问或标记"待确认项"，不得擅自脑补成正式设定。

## 生产阶段
本项目包含 5 个生产阶段：
1. 创意定义
2. 剧本开发
3. 生产规划
4. 资产生产
5. 视频生产

此外包含两条横切审核线：
- 业务审核
- 合规审核

## Agent 组织
本项目默认包含以下 8 个 Agent：
1. 主控 Agent：负责阶段判断、任务调度、状态管理、回退控制
2. 创意开发 Agent：负责创意访谈、创意收敛、创意设定输出
3. 剧本开发 Agent：负责梗概、分集、单集剧本、改稿
4. 生产规划 Agent：负责 15 秒分段、segment 任务单、衔接设计
5. 资产生产 Agent：负责资产清单、复用判断、生图 prompt、图片结果归档
6. 视频生产 Agent：负责视频 prompt、首尾帧策略、视频生成与结果归档
7. 业务审核 Agent：负责阶段质量判断、放行或退回
8. 合规审核 Agent：负责版权、人物权、敏感内容、平台限制等合规判断

## 主控调度规则
1. 主控 Agent 是总调度者。
2. 主控 Agent 必须先判断当前项目处于哪个阶段，再调用对应 Agent 或 Skill。
3. 前 3 个阶段必须严格串行：
   - 创意定义
   - 剧本开发
   - 生产规划
4. 后 2 个阶段允许有限并行：
   - 资产生产
   - 视频生产
5. 并行前提是上游正式文件已经 approved。
6. 任一审核失败时，主控必须明确回退到最近一个可修改节点，而不是模糊重试。
7. 主控不得越权替代专业 Agent 完成其核心职责。

## 审核门禁
以下节点必须经过业务审核：
- 创意设定完成后
- 剧本完成后
- segment 总表完成后
- 图片资产生成后
- 视频结果生成后

以下节点必须经过合规审核：
- 创意设定完成后
- 剧本完成后
- 资产生成前和生成后
- 视频生成前和生成后

若合规审核失败，必须优先回退，不得继续进入高成本生成阶段。

## 文件与目录约定
- `CLAUDE.md`：项目总规则与总调度原则
- `.claude/agents/`：项目级 Agent 定义
- `.claude/skills/`：项目级 Skills 及 supporting files
- `.claude/settings.json`：项目级共享设置
- `project_data/series/`：系列级设定与 Bible
- `project_data/episodes/epXX/`：单集剧本、segments、assets、videos、reviews
- `docs/`：架构、流程、命名规范等说明文档
- 外部工具接入统一通过 MCP 完成

## 文件化原则
所有正式产物必须：
- 使用固定结构
- 带版本号
- 带确认状态
- 明确待确认项

确认状态统一使用：
- draft
- in_review
- approved
- rejected

## 正式产物优先级
本项目优先生产以下正式文件：
- 创意设定文件
- 剧本文件
- segment 总表
- 单段任务文件
- 资产清单
- 图片结果归档
- 视频结果归档
- 业务审核结论
- 合规审核结论
- 阶段交接摘要

## Agent 工具权限规则
1. 主控 Agent、业务审核 Agent、合规审核 Agent 不得直接调用 Dream Maker。
2. 创意开发 Agent、剧本开发 Agent、生产规划 Agent 只能读写项目文件，不得调用执行型生成工具。
3. 资产生产 Agent 只能调用图像相关工具。
4. 视频生产 Agent 只能调用视频相关工具。
5. 所有外部模型调用统一通过 MCP tools 接入。
6. 所有规则、清单、模板优先通过项目文件、skills supporting files 或 MCP resources/prompts 提供。

## 质量原则
1. 不接受"差不多能用"的中间结果直接流入下一阶段。
2. 任何 Agent 在输出阶段结果时，都必须明确：
   - 已确认内容
   - 待确认内容
   - 风险点
   - 下一步建议
3. 如果当前结果缺失关键字段，优先退回补全，不强行放行。

## 合规原则
1. 对版权、IP、真实人物、肖像、声音、敏感题材、平台限制保持严格前置检查。
2. 一旦存在高风险项，优先打回，不进入图片或视频生成。
3. 合规判断结果必须文件化，并说明风险类型和修改建议。

## 实施约定
1. `CLAUDE.md` 只保存项目总规则与总调度原则。
2. 各 Agent 详细职责放在 `.claude/agents/`
3. 各阶段方法论放在 `.claude/skills/`
4. 模板、清单、规则放在 supporting files 或 MCP resources/prompts 中。
5. 需要长期共享的项目规则优先写入本文件；只适用于局部目录或局部任务的规则不要堆进本文件。
